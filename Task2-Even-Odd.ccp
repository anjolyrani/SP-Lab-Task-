#include<stdio.h>
int main()
{

    int x, y, sum;

    printf("\nThe number of  Even: ");
    scanf("%d", &x);


    printf("\nThe number of Odd: ");
    scanf("%d", &y);

    sum = x + y;


    printf("\nSum of the two integers = %d\n", sum);
    return 0;
}
